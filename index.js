function addingEventListener() {
}
function addingEventListener() {
    const input = document.getElementById('button'); // Replace 'button' with the actual ID of your element
    input.addEventListener('click', function() {
        alert('I was clicked!');
        // Additional code to handle the click event
    });
}

// Call addingEventListener to bind the event listener
addingEventListener();
